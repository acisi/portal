ReadLines(filename) - Прочитать все строки в массив строк
WriteLines(filename,lines) - Записать все строки в файл
CheckIP (_ip) - Проверка и адаптация IP адреса в случае ошибки возвращается 0.0.0.0 или если IP-неневерен, или попадает в диапазоны локальных сетей